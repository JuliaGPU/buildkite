# gpuci has 2 AMD EPYC 7402 CPUs, 48 threads in total, with 504GB of RAM
#
# This agent shares its RTX 5000 with gpuci.16. CI jobs are mostly compile-bound
# and leave the GPU idle, and 16GB of VRAM fits two jobs. The lower priority
# makes jobs prefer agents with a dedicated GPU.

services:
  buildkite:
    hostname: gpuci.19
    build:
      context: ../../image/
      args:
        base: nvidia/cuda:13.3.0-devel-ubuntu24.04
    runtime: nvidia
    environment:
      NVIDIA_DRIVER_CAPABILITIES: "compute,utility"
      NVIDIA_VISIBLE_DEVICES: "GPU-ec68e28e-d7c6-21eb-8223-88cb012eae5c"
      JULIA_CPU_THREADS: 2
      LD_LIBRARY_PATH: ""
    pid: "host" #  NVIDIA/gpu-monitoring-tools#63
    env_file: ../token.env
    command:
      - start
      - --disconnect-after-job
      - --hooks-path=/hooks
      - --tags=queue=cuda,cap=sm_75,gpu=rtx5000,cap=recent,shared=true
      - --name=gpuci.19
      - --priority=3
    volumes:
      - /home/buildkite/gpuci.19:/root
